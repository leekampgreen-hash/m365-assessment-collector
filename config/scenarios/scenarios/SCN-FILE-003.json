{
  "schema_version": "1.0",
  "scenario_id": "SCN-FILE-003",
  "name": "Delete / cleanup test file in actor OneDrive",
  "description": "test-user-01 deletes the test file created by SCN-FILE-001. The deletion is scoped to the file identified by the correlation token.",
  "domain": "FILES",
  "action_type": "DELETE_FILE",
  "actor_required": "test-user-01",
  "peer_actor_required": null,
  "no_recipient": true,
  "required_delegated_permissions": ["Files.ReadWrite"],
  "expected_observable_sources": ["G01-006", "G01-005"],
  "observability_classification": "INDIRECTLY_OBSERVABLE",
  "correlation_strategy": "Deletes only the file matching the correlation token.",
  "correlation_token_field": "file_name",
  "cleanup_required": false,
  "cleanup_behavior": "NO_CLEANUP_REQUIRED",
  "destructive": false,
  "risk": "LOW",
  "enabled": false,
  "permission_pack": "PACK-FILES",
  "notes": "destructive=false because the deletion is scoped to a scenario-created artifact."
}